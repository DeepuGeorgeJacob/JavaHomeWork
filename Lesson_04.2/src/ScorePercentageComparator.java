import java.util.Comparator;

public class ScorePercentageComparator implements Comparator<ScoredStudent> {
    @Override
    public int compare(ScoredStudent o1, ScoredStudent o2) {
        return 0;
    }
}
